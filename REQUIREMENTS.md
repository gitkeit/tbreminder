# tbreminder 要件定義書

Windhawk tbreminder モジュールの開発者向け要件定義書。

## 1. 目的

スケジュール管理とリマインダー通知。
対象は Windows デスクトップユーザー。
toast 通知（Toast通知）とタスクバー次回予定表示を行う。

## 2. 機能要件

### FR-01: スケジュールファイル管理

1. JSON 配列形式（JSON array）でエントリを定義する。
2. 3形式の時刻指定をサポートする。
   - `HH:MM`（毎日繰り返し）
   - `YYYY-MM-DD HH:MM`（一回きり）
   - `*:MM`（毎時）
3. コメント記号 `#` および `//` に対応する。
4. 末尾カンマを許容する。
5. BOM（バイト順マーク）を対応する。
6. UTF-8 と ANSI を自動判別する。
7. 最大 32 ファイルを読み込む。
8. ファイル単位でエントリを管理する。
   破損時は前回の有効なエントリを保持する。
9. 同一エントリの重複を排除する。

### FR-02: 通知機能

1. 3方式の通知を提供する。
   - `custom`（カスタム通知）
   - `system`（システム通知）
   - `both`（両方）
2. キュー（通知待ち行列）は最大 10 件とする。
3. N 分前通知（アドバンス通知）に対応する。
4. 後追い発火に対応する。
5. 起動時猶予発火に対応する。
6. 重複防止機能を実装する。
   発火キーで 1 分単位バケット（時間枠）管理を行う。

### FR-03: タスクバーオーバーレイ

1. 直近 N 件を表示する（1～5 件）。
2. 3配置をサポートする。
   - `tray-left`（システムトレイ左側）
   - `taskbar-center`（タスクバー中央）
   - `taskbar-left`（タスクバー左側）
3. 自動縮小（auto-shrink）に対応する。
4. 混雑時は非表示とし、フラッシュで通知する。
5. ホバー（マウスオーバー）時に表示を変更する。
6. クリック操作に対応する。

### FR-04: ファイル監視

1. mtime（更新時刻）の変化を検知する。
2. UTF-8 と ANSI の両方に対応する。
3. ファイルサイズ上限は 10MB とする。
4. 不正な JSON はスキップし、ログを出力する。

### FR-05: エラーハンドリング

1. ファイル不在時はスキップし、ログを出力する。
2. 時刻の形式が不正な場合はスキップし、ログを出力する。
3. 通知失敗時はリトライ（再試行）を行う。
4. スレッド（スレッド）の異常時は安全にシャットダウンする。

## 3. 非機能要件

### NFR-01: パフォーマンス

1. フォントキャッシュを使用し、再描画を抑制する。
2. shared_ptr（共有ポインタ）によるスナップショット（瞬間値保持）
   で読み取りロックを回避する。
3. 分境界時刻に 2 秒の起床猶予を設ける。

### NFR-02: 信頼性

1. 単一ワーカースレッドで処理を集約する。
2. explorer.exe が存在する場合は UI 委譲を優先する。
3. クリティカルセクション（排他区間）を分離して
   競合を回避する。

### NFR-03: 互換性

1. Windows 10 および Windows 11 に対応する。
2. x86 および x86-64 アーキテクチャに対応する。
3. DPI（解像度密度）変動に対応する。

### NFR-04: セキュリティ

1. ShellExecute（外部プロセス起動）は別スレッドで実行する。
2. 機密情報のログ出力を行わない。

## 4. 仕様

### データモデル

1. Entry（エントリ）のフィールド。
   - kind: 種別（daily/once/hourly）
   - hour: 時
   - minute: 分
   - year: 年
   - month: 月
   - day: 日
   - message: メッセージ本文
   - rawTime: 元の時刻文字列
   - sourceFile: 読み込み元ファイルパス

### 状態遷移

1. 起動 → ファイル読み込み → ソート（整列）
   → ポーリング（周期確認） → 発火 → 通知

### スレッドモデル

1. explorer.exe 経由の場合: UI スレッド＋ワーカースレッド
2. Windhawk 経由の場合: ワーカースレッドのみ

## 5. 既知の制約

1. FAT ファイルシステムの mtime 精度は 2 秒である。
2. DPI 変動時にフォントキャッシュが追従できない場合がある。
3. タスクバーの変更時に再配置が確実に実行されない場合がある。
4. 垂直タスクバー（縦配置）の位置計算は簡略化している。
